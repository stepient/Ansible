---
- name: Test playbook
  hosts: all
  gather_facts: no
  tasks:
  
    - name: Write message
      debug:
        msg: Hello World!
        
    - name: whoami
      command: whoami
      register: whoami
      
    - debug: var=whoami
        
    - name: /etc/hosts
      lineinfile:
        path: /etc/hosts
        state: absent
        regexp: 'test'
      

    
