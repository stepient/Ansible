---
- name: Test playbook
  hosts: all
  gather_facts: no
  tasks:
  
    - name: Write message
      debug:
        msg: Hello World!
        
    - name: whoami
      command: whoami
      register: whoami
      
    - debug: var=whoami
        
    - name: Create a directory if it does not exist
      ansible.builtin.file:
        path: /etc/some_directory
        state: directory
        mode: '0755'
    
